import React from 'react';
import { Box, Text, VStack, Card, CardBody } from '@chakra-ui/react';
import ReservationItem from './ReservateinItem';

export default function ReservationList() {
  return (
    <ReservationItem />
  );
}
